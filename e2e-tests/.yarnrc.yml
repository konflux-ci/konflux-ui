# Yarn Berry configuration for e2e-tests
# Using node-modules linker for compatibility with Cypress
nodeLinker: node-modules

# Enable compression for the cache
compressionLevel: mixed

# Specify the default install state path
enableGlobalCache: false
