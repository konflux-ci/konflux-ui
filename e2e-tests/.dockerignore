# Dependencies (rebuilt in container)
node_modules

# Cypress artifacts (not needed in image)
cypress/downloads
cypress/screenshots
cypress/videos

# Environment files (may contain secrets)
.env
.env.*
*.local

# Logs
*.log
cypress-log.txt

# Yarn cache (rebuilt in container)
.yarn/cache
.yarn/install-state.gz

# OS files
.DS_Store

# Git
.git