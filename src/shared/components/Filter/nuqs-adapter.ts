/**
 * Re-exports the nuqs React Router v6 adapter.
 *
 * Wrap your app (or test harness) with `NuqsAdapter` to enable URL-based
 * filter state management via nuqs.
 */
export { NuqsAdapter } from 'nuqs/adapters/react-router/v6';
